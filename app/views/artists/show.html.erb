<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title><%= @artist.name %></title>
  </head>
  <body>
    <h1><%= @artist.name %></h1>
    <ul>
      <% @artist.songs.each do |s| %>
        <li><%= link_to s.title, song_path(s) %></li>
      <% end %>
    


  </body>
</html>
