<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Artist Index</title>
  </head>
  <body>
    <h1>Artist List</h1>

    <% @artists.each do |a| %>
      <li><%= link_to a.name, artist_path(a) %>, <%= a.song_count %> songs</li>
    <% end %>
  </body>
</html>
