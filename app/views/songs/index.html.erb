<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>All Songs</title>
  </head>
  <body>
    <h1>Songs List</h1>

    <% @songs.each do |s| %>
    <li><%= link_to s.artist_name + " - " + s.title, song_path(s) %>
    <% end %>
  </body>
</html>
